from Env.kuhn_poker_env import Kuhn_Poker
import numpy as np
env = Kuhn_Poker()
api = env.start_play()
next(api)
api.send(0)

bool([1])

data = np.array([1])

data > 0
idx = data <= 0
idx
data[idx]

data[]
